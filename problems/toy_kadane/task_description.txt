**OBJECTIVE**: Write a Python function that finds the **maximum sum of a contiguous subarray** in a given list of integers.

**INPUT**: The returned solver receives a list of integers (positive and negative).

**OUTPUT**: The solver returns a single integer — the maximum contiguous subarray sum.

**EXAMPLES**:
- `[-2, 1, -3, 4, -1, 2, 1, -5, 4]` → `6` (subarray `[4, -1, 2, 1]`)
- `[1]` → `1`
- `[5, 4, -1, 7, 8]` → `23` (entire array)
- `[-1, -2, -3]` → `-1` (least negative element)

**RULES**:
- The input list is always non-empty (1 to 10000 elements).
- Elements are integers in range [-10000, 10000].
- Must handle all-negative arrays correctly (return the largest single element).
- No external libraries needed — pure Python is sufficient.

**HINT**: Kadane's algorithm solves this in O(n) time.

**CONTRACT — READ CAREFULLY**: `entrypoint` is called with **zero arguments** by the runtime. It must build and return a solver function (a closure); the runtime then calls that returned function once per test case with a `nums: list[int]` argument. Do NOT change `entrypoint`'s own signature to take `nums` directly — it never receives it, and doing so will crash immediately with a TypeError before any test case runs.

OUTPUT:
```python
def entrypoint():
    def solve(nums: list[int]) -> int:
        # Your implementation here
        return result
    return solve
```
